# Fundamental-combining

This repository will show case different programs that combine the concepts which were previously introduced

*Calculator.java

Java syntax requires a .equals rather than ==, this is why the program wasn't outputting anything because the wrong comparison syntax was being used. .equals has to be used because the conditions are judging a string/object and == can only be used on a primite such as an integer. 